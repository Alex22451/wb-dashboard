# Project Rules Report

Date: 2026-07-15

## Result

- Added the persistent repository instructions in `AGENTS.md`.
- Defined permission boundaries, critical-fix handling, data correctness rules,
  WB API safeguards, logic testing, database changes, UI checks, Git delivery,
  deployment behavior, and reporting requirements.
- API keys and existing unrelated local files were not changed.

## Verification

- Markdown and Git whitespace checks passed.
- Logic tests and a deployment check were not required for documentation-only
  changes.

## Delivery

- Rules commit: `661adeb` (`Add persistent project rules`).
- Pushed to `main`.
